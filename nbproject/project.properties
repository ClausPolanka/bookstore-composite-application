build.classes.dir=${build.dir}/jar
build.dir=build
build.generated.dir=${build.dir}/generated
com.sun.jbi.routing=true
com.sun.jbi.routing.bc.autoconnect=true
com.sun.jbi.sa.internal.routing=true
dist.dir=dist
dist.jar=${dist.dir}/bookstore-composite-application.zip
jbi.content.additional=${reference.bookstore-businessprocess.dist_se}
jbi.content.component=sun-bpel-engine
jbi.service-assembly.description=Represents the Service Assembly of bookstore-composite-application
jbi.service-assembly.id=bookstore-composite-application
jbi.service-unit.description=Represents this Service Unit
meta.inf=${source.root}/conf
project.bookstore-businessprocess=../bookstore-businessprocess
reference.bookstore-businessprocess.dist_se=${project.bookstore-businessprocess}/build/SEDeployment.jar
resource.bookstore-businessprocess=${project.bookstore-businessprocess}/setup
source.encoding=UTF-8
source.root=src
src.build.dir=${source.root}/jbiServiceUnits
src.dir=${source.root}/jbiasa
test.dir=test
test.results.dir=${basedir}/test/results
